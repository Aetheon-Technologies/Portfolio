# Sensitive files — never commit
.env
reset-admin.php
*.log
*.sql.bak

# Uploads — track images so they persist across Railway deploys
# Only exclude large or sensitive files if needed

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
*.swp
